name: Wire Nexus People Panel
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/wire-nexus-people-panel.yml
permissions:
  contents: write
jobs:
  wire:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Wire people panel assets
        run: |
          python - <<'PY'
          from pathlib import Path
          path=Path('apps/nexus-graph-preview/relationship-tree/index.html')
          text=path.read_text(encoding='utf-8')
          css='<link rel="stylesheet" href="./nexus-people-panel.css" />'
          js='<script defer src="./nexus-people-panel.js"></script>'
          if css not in text:
              text=text.replace('<link rel="stylesheet" href="./nexus-project-switcher.css" />','<link rel="stylesheet" href="./nexus-project-switcher.css" />\n    '+css,1)
          if js not in text:
              text=text.replace('<script defer src="./nexus-project-switcher.js"></script>','<script defer src="./nexus-project-switcher.js"></script>\n    '+js,1)
          path.write_text(text,encoding='utf-8')
          PY
          node --check apps/nexus-graph-preview/relationship-tree/nexus-people-panel.js
          grep -q 'nexus-people-panel.css' apps/nexus-graph-preview/relationship-tree/index.html
          grep -q 'nexus-people-panel.js' apps/nexus-graph-preview/relationship-tree/index.html
      - name: Commit wiring and cleanup
        run: |
          rm .github/workflows/wire-nexus-people-panel.yml
          git config user.name "NOSMO Build Bot"
          git config user.email "actions@users.noreply.github.com"
          git add apps/nexus-graph-preview/relationship-tree/index.html .github/workflows/wire-nexus-people-panel.yml
          git commit -m "Wire Nexus project people panel"
          git push origin HEAD:main