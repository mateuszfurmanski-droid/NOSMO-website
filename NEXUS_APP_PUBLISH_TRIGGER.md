# Nexus application publishing trigger

This temporary file opens a controlled pull request so the existing Nexus application build can be published under `/apps/nexus/` and connected to the thematic Nexus Dock.
