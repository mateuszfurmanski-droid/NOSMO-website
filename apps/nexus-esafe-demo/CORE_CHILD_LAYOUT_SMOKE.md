# CORE child layout smoke test

PR #27 visual-only verification trigger.

Validate on phone / Fold:
- category nodes remain draggable and pinch/pan still work;
- CORE child nodes expand toward available viewport space;
- parent-to-child connector lines stay attached;
- long child titles clamp without overlapping neighboring branches;
- resize/orientation recalculates child direction;
- Timeline ACTIVE/FUTURE state remains unchanged.
