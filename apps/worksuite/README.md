# NOSMO WorkSuite public entry

`/apps/worksuite/` is the canonical Phase 1 public product entry.

During the staged rebrand it forwards to the existing `/apps/doorflow/` deployment so old runtime/deployment assumptions remain intact. New Nexus launchers should target `/apps/worksuite/`; old `/apps/doorflow/` links remain valid for compatibility.
